import { FormLogin } from "../features/auth/components/FormLogin";

const LoginPage = () => {
    return (
        <div>
            <h2>Login Page</h2>
            <FormLogin />
        </div>
    );
};

export default LoginPage;