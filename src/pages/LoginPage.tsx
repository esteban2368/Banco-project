import { FormLogin } from "../features/auth/components/FormLogin";

const LoginPage = () => {
    return (
        <section className="">
            <h2>Login Page</h2>
            <FormLogin />
        </section>
    );
};

export default LoginPage;