import { TransferForm } from "../features/transfer/components/TransferForm";

export const TransferPage = () => {
    return (
        <div>
            <h1>Transferencia</h1>
            <TransferForm />
        </div>
    );
};